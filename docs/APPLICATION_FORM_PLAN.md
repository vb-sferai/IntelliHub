# План реализации анкеты кандидата с интеграцией Google Sheets

## 📋 Обзор задачи
Создать онлайн-анкету для кандидатов на вакансию PM с автоматической записью результатов в Google Таблицы. Анкета должна соответствовать дизайну страницы вакансии и быть переиспользуемой для будущих вакансий.

## 🎯 Архитектурное решение

### 1. **Структура компонентов**
```
src/
├── components/
│   └── ApplicationForm/       # Переиспользуемый компонент формы
│       ├── components/
│       │   ├── ApplicationForm.tsx
│       │   ├── FormField.tsx
│       │   ├── TextInput.tsx
│       │   ├── TextArea.tsx
│       │   ├── RadioGroup.tsx
│       │   └── SubmitButton.tsx
│       ├── hooks/
│       │   └── useGoogleSheets.ts
│       ├── types.ts
│       └── index.ts
│
├── pages/
│   └── JobPages/
│       └── ApplicationPage/   # Универсальная страница анкеты
│           ├── components/
│           │   └── ApplicationPage.tsx
│           └── index.ts
│
└── services/
    └── googleSheets/
        ├── api.ts
        └── config.ts
```

### 2. **Маршрутизация**
- Создать новый роут: `/jobs/pm/apply`
- В будущем: `/jobs/{jobId}/apply` для динамических вакансий

## 📝 Реализация формы

### Поля анкеты:
1. **experience** - Расскажите про свой опыт (textarea, required)
2. **motivation** - Почему вам интересно поработать вместе? (textarea, required)
3. **aiUsage** - Как вы используете AI в повседневной жизни? (textarea, required)
4. **successProject** - Расскажите о вашем самом успешном проекте (textarea, required)
5. **failureProject** - Расскажите о менее удачном проекте (textarea, required)
6. **englishLevel** - Владение английским (radio 1-10, required)
7. **expectedCompensation** - Ожидаемый уровень компенсации (text, required)
8. **location** - Где вы живете? (text, required)
9. **fullName** - Как вас зовут (text, required)
10. **telegram** - Ваш никнейм в Телеграме (text, required)
11. **additionalInfo** - Что-то еще (textarea, optional)

### Дизайн формы:
- Использовать тот же MeshGradient фон, что и на странице вакансии
- Белая карточка формы с закругленными углами на градиентном фоне
- Стилизация полей в соответствии с дизайном сайта:
  - Синий цвет для фокуса (#005EE0)
  - Серые рамки для полей ввода
  - Responsive дизайн с правильными отступами

## 🔗 Интеграция с Google Sheets

### Подход: Google Apps Script Web App
1. **Создать Google Apps Script** в целевой таблице:
   - Обработчик POST запросов
   - Автоматическая запись данных в таблицу
   - Добавление timestamp для каждой заявки
   - Возврат успешного/ошибочного статуса

2. **Структура данных в таблице:**
   - Колонка A: Дата и время заявки
   - Колонка B: Имя и фамилия
   - Колонка C: Telegram
   - Колонка D: Локация
   - Колонка E: Уровень английского
   - Колонка F: Ожидаемая компенсация
   - Колонка G: Опыт
   - Колонка H: Мотивация
   - Колонка I: Использование AI
   - Колонка J: Успешный проект
   - Колонка K: Неудачный проект
   - Колонка L: Дополнительная информация
   - Колонка M: Вакансия (для будущих вакансий)

3. **Безопасность:**
   - CORS настройки в Google Apps Script
   - Валидация данных на клиенте
   - Rate limiting (защита от спама)
   - Honeypot поле для защиты от ботов

## 🎨 UI/UX детали

### Страница анкеты будет включать:
1. **Хедер** - заголовок "Анкета на позицию Product Manager" с логотипом
2. **Прогресс-индикатор** - показывает заполнение формы
3. **Форма** с группировкой полей:
   - Блок "О вас" (имя, контакты, локация)
   - Блок "Ваш опыт" (опыт работы, проекты)
   - Блок "Навыки" (английский, AI)
   - Блок "Ожидания" (компенсация, мотивация)
4. **Кнопка отправки** с состояниями:
   - Обычное состояние
   - Загрузка (с анимацией)
   - Успех (переход на страницу благодарности)
   - Ошибка (с возможностью повтора)

### После отправки:
- Показать страницу благодарности
- Предложить вернуться к описанию вакансии
- Автоматическое уведомление HR (опционально)

## 🔄 Переиспользуемость

### Для будущих вакансий:
1. **Конфигурационный подход:**
   - Создать `jobConfigs` с настройками для каждой вакансии
   - Динамическая генерация полей формы
   - Разные Google Sheets для разных вакансий

2. **Структура конфига вакансии:**
```typescript
interface JobConfig {
  id: string;
  title: string;
  formFields: FormField[];
  googleSheetId: string;
  successMessage: string;
}
```

## 📦 Необходимые зависимости
- react-hook-form (управление формой)
- zod (валидация)
- react-hot-toast (уведомления)

## 🚀 Порядок реализации

1. **Создать базовые компоненты формы** (TextInput, TextArea, RadioGroup)
2. **Создать главный компонент ApplicationForm** с логикой валидации
3. **Настроить Google Apps Script** в целевой таблице
4. **Создать страницу анкеты** с дизайном как у страницы вакансии
5. **Добавить роутинг** для новой страницы
6. **Интегрировать отправку данных** в Google Sheets
7. **Добавить состояния загрузки и обработку ошибок**
8. **Обновить CTA кнопку** на странице вакансии для перехода на анкету
9. **Тестирование** всего флоу от заполнения до записи в таблицу

## 🔧 Google Apps Script код

```javascript
function doPost(e) {
  try {
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    const data = JSON.parse(e.postData.contents);

    const row = [
      new Date().toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' }),
      data.fullName,
      data.telegram,
      data.location,
      data.englishLevel,
      data.expectedCompensation,
      data.experience,
      data.motivation,
      data.aiUsage,
      data.successProject,
      data.failureProject,
      data.additionalInfo || '',
      data.jobPosition || 'Product Manager'
    ];

    sheet.appendRow(row);

    return ContentService
      .createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet() {
  return ContentService
    .createTextOutput(JSON.stringify({ status: 'Google Sheets API is running' }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

## 📊 Анализ полей из скриншотов

### Обязательные поля (помечены *):
1. Расскажите про свой опыт
2. Почему вам интересно поработать вместе?
3. Как вы используете AI в повседневной жизни?
4. Расскажите о вашем самом успешном проекте
5. Расскажите о менее удачном проекте
6. Владение английским (шкала 1-10)
7. Ожидаемый уровень компенсации
8. Где вы живете?
9. Как вас зовут
10. Ваш никнейм в Телеграме

### Опциональные поля:
11. Что-то еще

Это решение обеспечит красивую, функциональную анкету с автоматической записью в Google Sheets и возможностью легкого переиспользования для будущих вакансий.
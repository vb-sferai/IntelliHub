# Руководство по проверке и отладке счетчика Яндекс.Метрики для страницы /baza

## Информация о счетчиках

- **Основной счетчик**: 105383064 (установлен в index.html)
- **Счетчик для /baza**: 105383668 (инициализируется в BasePage.tsx)

## Как проверить работу счетчика 105383668

### 1. Проверка в браузере (Chrome/Firefox)

1. **Откройте страницу /baza в браузере**
   ```bash
   npm run dev
   # Перейдите на http://localhost:5173/baza
   ```

2. **Откройте DevTools (F12) и проверьте консоль**

   Должны увидеть сообщения:
   - `✅ Метрика инициализирована: 105383668`
   - Если видите `⚠️ Яндекс.Метрика не загружена` - значит есть проблема

3. **Проверьте вкладку Network**
   - Фильтр: `metrika` или `yandex`
   - Должны быть запросы к:
     - `https://mc.yandex.ru/metrika/tag.js`
     - `https://mc.yandex.ru/watch/105383668`
   - Статус должен быть 200 OK

4. **Проверьте в консоли браузера**
   ```javascript
   // Введите эти команды в консоль
   window.ym
   // Должна вернуть функцию

   // Проверить отправку тестовой цели
   window.ym(105383668, 'reachGoal', 'TEST_GOAL')
   ```

### 2. Проверка через расширение Яндекс.Метрики

1. **Установите расширение Yandex Metrica Tag Assistant**
   - Chrome: https://chrome.google.com/webstore/detail/yandex-metrica-tag-assist/
   - Firefox: https://addons.mozilla.org/ru/firefox/addon/yandex-metrica-tag-assistant/

2. **Откройте страницу /baza с включенным расширением**
   - В панели расширения должно показать:
     - Счетчик 105383064 (основной)
     - Счетчик 105383668 (для /baza)

3. **Проверьте статус счетчиков**
   - Зеленая галочка = работает корректно
   - Красный крестик = есть ошибки

### 3. Проверка в интерфейсе Яндекс.Метрики

1. **Войдите в Яндекс.Метрику**
   - https://metrika.yandex.ru/dashboard?id=105383668

2. **Проверьте раздел "Онлайн"**
   - Откройте страницу /baza в браузере
   - Подождите 1-2 минуты
   - В разделе "Онлайн" должен появиться посетитель

3. **Проверьте раздел "Отчеты" → "Стандартные отчеты" → "Содержание"**
   - Должны видеть страницу `/baza` в списке
   - Может потребоваться подождать 10-15 минут для обновления данных

### 4. Использование режима отладки

Добавьте временный код для отладки в `BasePage.tsx`:

```javascript
useEffect(() => {
    // Добавьте отладочный код
    console.log('🔍 Проверка Яндекс.Метрики:');
    console.log('window.ym существует?', typeof (window as any).ym !== 'undefined');

    // Инициализируем счетчик
    initMetrika(105383668);

    // Проверяем через 2 секунды
    setTimeout(() => {
        if ((window as any).ym) {
            console.log('✅ Отправляем тестовую цель');
            (window as any).ym(105383668, 'reachGoal', 'PAGE_VIEW_BAZA');
        } else {
            console.log('❌ window.ym не найден');
        }
    }, 2000);
}, []);
```

## Возможные проблемы и решения

### Проблема 1: Скрипт Метрики не загружается

**Симптомы**: В консоли видите `⚠️ Яндекс.Метрика не загружена`

**Решение**: Проверьте, что основной скрипт в index.html загружается корректно

### Проблема 2: Счетчик не инициализируется

**Симптомы**: Нет сообщения об инициализации в консоли

**Решение**: Убедитесь, что функция `initMetrika` вызывается в `useEffect`

### Проблема 3: Блокировка AdBlock

**Симптомы**: Запросы к Яндекс.Метрике блокируются

**Решение**:
- Отключите AdBlock для localhost
- Добавьте localhost в исключения AdBlock
- Используйте браузер в режиме инкогнито без расширений

### Проблема 4: Неправильный ID счетчика

**Симптомы**: В Яндекс.Метрике не видно данных

**Решение**:
- Убедитесь, что ID счетчика 105383668 существует
- Проверьте права доступа к счетчику в настройках Яндекс.Метрики

## Тестирование целей

Для проверки работы целей добавьте этот код в кнопки на странице:

```javascript
import { trackGoal } from '../../../utils/analytics';

// В обработчике клика
const handleButtonClick = () => {
    // Отправляем цель для счетчика /baza
    trackGoal(105383668, 'BUTTON_CLICK', {
        button: 'purchase',
        stream: activeStream
    });

    // Ваша логика
};
```

## Проверка через curl

Можно проверить доступность счетчика напрямую:

```bash
# Проверка доступности скрипта
curl -I https://mc.yandex.ru/metrika/tag.js

# Должен вернуть HTTP/1.1 200 OK
```

## Контрольный чек-лист

- [ ] Страница /baza открывается без ошибок
- [ ] В консоли браузера нет ошибок JavaScript
- [ ] Видно сообщение `✅ Метрика инициализирована: 105383668`
- [ ] В Network видны запросы к Яндекс.Метрике
- [ ] Расширение Tag Assistant показывает счетчик
- [ ] В интерфейсе Яндекс.Метрики видны посетители
- [ ] Цели отправляются корректно (проверить через консоль)

## Контакты для помощи

Если счетчик все еще не работает:
1. Проверьте настройки счетчика в Яндекс.Метрике
2. Убедитесь, что счетчик 105383668 активен и не заблокирован
3. Обратитесь в поддержку Яндекс.Метрики: https://yandex.ru/support/metrica/
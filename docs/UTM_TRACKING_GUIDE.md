# Руководство по UTM-трекингу

Это руководство описывает систему трекинга UTM-параметров и аналитики на сайте SFER.AI.

## Оглавление

1. [Что такое UTM-параметры](#что-такое-utm-параметры)
2. [Как работает система трекинга](#как-работает-система-трекинга)
3. [Настройка целей в Яндекс.Метрике](#настройка-целей-в-яндексметрике)
4. [Обновление Google Apps Script](#обновление-google-apps-script)
5. [Использование в коде](#использование-в-коде)
6. [Примеры UTM-ссылок](#примеры-utm-ссылок)
7. [Тестирование](#тестирование)

---

## Что такое UTM-параметры

UTM-параметры — это специальные метки в URL, которые помогают отслеживать источники трафика и эффективность рекламных кампаний.

### Основные UTM-параметры:

- **utm_source** — источник трафика (google, yandex, telegram, facebook)
- **utm_medium** — тип трафика (cpc, social, email, referral, organic)
- **utm_campaign** — название кампании (base_workshop_nov, black_friday_2024)
- **utm_term** — ключевое слово (для контекстной рекламы)
- **utm_content** — вариант объявления (banner_a, text_ad_v2)

### Пример UTM-ссылки:

```
https://ru.sfer.ai/baza?utm_source=telegram&utm_medium=social&utm_campaign=base_workshop_nov&utm_content=video_promo
```

---

## Как работает система трекинга

### Автоматический сбор данных

При заходе пользователя на сайт автоматически:

1. **Извлекаются UTM-параметры** из URL
2. **Сохраняются в sessionStorage** (на всю сессию)
3. **Отправляются в Яндекс.Метрику** при каждом просмотре страницы
4. **Прикрепляются к заявкам** при отправке форм

### Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│  Пользователь переходит по ссылке с UTM                     │
│  https://ru.sfer.ai/?utm_source=telegram&utm_medium=social  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  useAnalytics Hook (инициализация при загрузке приложения)  │
│  - Извлекает UTM из URL                                     │
│  - Сохраняет в sessionStorage                               │
│  - Отслеживает навигацию между страницами                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  SessionStorage хранит:                                      │
│  {                                                           │
│    "utm_source": "telegram",                                │
│    "utm_medium": "social",                                  │
│    "utm_campaign": "base_workshop_nov",                     │
│    "referrer": "https://t.me/...",                          │
│    "landing_page": "/baza",                                 │
│    "timestamp": "2024-11-18T12:34:56.789Z"                  │
│  }                                                           │
└────────────────────────┬────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
          ▼                             ▼
┌─────────────────────┐       ┌─────────────────────────┐
│  Просмотр страницы  │       │  Клик по кнопке CTA     │
│  trackPageView()    │       │  trackGoal()            │
└──────────┬──────────┘       └──────────┬──────────────┘
           │                             │
           ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│  Яндекс.Метрика (ID: 105383064)                             │
│  - События просмотра страниц с UTM                          │
│  - Цели (workshop_purchase_click, job_application_submitted)│
│  - Параметры визитов                                        │
└─────────────────────────────────────────────────────────────┘

                         ┌──────────────┐
                         │              │
                         ▼              │
┌─────────────────────────────────────────────────────────────┐
│  Отправка формы заявки                                      │
│  - Добавляет UTM к данным формы                             │
│  - Отправляет в Google Sheets                               │
│  - Отправляет цель в Яндекс.Метрику                         │
└─────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────┐
│  Google Sheets                                              │
│  Сохраняет: личные данные + UTM-параметры + timestamp       │
└─────────────────────────────────────────────────────────────┘
```

---

## Настройка целей в Яндекс.Метрике

### Текущие цели:

1. **`job_application_submitted`** — Отправка заявки на вакансию
   - Параметры: `position`, UTM-метки

2. **`workshop_purchase_click`** — Клик по кнопке покупки воркшопа
   - Параметры: `workshop`, `tier`, `price`, `link`, UTM-метки

3. **`page_view`** — Просмотр страницы
   - Параметры: `page_path`, UTM-метки

### Как настроить цели в интерфейсе Метрики:

1. Перейдите в **Яндекс.Метрику** → **Настройки** → **Цели**
2. Нажмите **Добавить цель**
3. Выберите условие **JavaScript-событие**
4. Укажите идентификатор: `job_application_submitted`, `workshop_purchase_click` и т.д.
5. Сохраните цель

После настройки вы сможете видеть конверсии в отчётах Метрики.

---

## Обновление Google Apps Script

Для корректной работы необходимо обновить Google Apps Script, чтобы он принимал и сохранял UTM-параметры.

### Шаг 1: Откройте скрипт

1. Перейдите в Google Sheets с заявками
2. **Расширения** → **Apps Script**

### Шаг 2: Обновите код

Найдите функцию `doPost` и замените её на следующий код:

```javascript
function doPost(e) {
  try {
    // Получаем данные из POST-запроса
    const data = JSON.parse(e.postData.contents);

    // Открываем активную таблицу
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();

    // Проверяем, есть ли заголовки (первая строка)
    if (sheet.getLastRow() === 0) {
      // Если таблица пустая, добавляем заголовки
      sheet.appendRow([
        'Timestamp',
        'Позиция',
        'ФИО',
        'Telegram',
        'Локация',
        'Опыт работы',
        'Уровень английского',
        'Использование AI',
        'Успешный проект',
        'Провальный проект',
        'Мотивация',
        'Ожидаемая компенсация',
        'Дополнительная информация',
        'UTM Source',
        'UTM Medium',
        'UTM Campaign',
        'UTM Term',
        'UTM Content',
        'Referrer',
        'Landing Page'
      ]);
    }

    // Добавляем новую строку с данными
    sheet.appendRow([
      data.timestamp || new Date().toISOString(),
      data.jobPosition || '',
      data.fullName || '',
      data.telegram || '',
      data.location || '',
      data.experience || '',
      data.englishLevel || '',
      data.aiUsage || '',
      data.successProject || '',
      data.failureProject || '',
      data.motivation || '',
      data.expectedCompensation || '',
      data.additionalInfo || '',
      data.utm_source || '',
      data.utm_medium || '',
      data.utm_campaign || '',
      data.utm_term || '',
      data.utm_content || '',
      data.referrer || '',
      data.landing_page || ''
    ]);

    // Возвращаем успешный ответ
    return ContentService.createTextOutput(JSON.stringify({
      status: 'success',
      message: 'Заявка успешно сохранена'
    })).setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    // Логируем ошибку
    Logger.log('Error: ' + error.toString());

    // Возвращаем ошибку
    return ContentService.createTextOutput(JSON.stringify({
      status: 'error',
      message: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}
```

### Шаг 3: Переразверните скрипт

1. Нажмите **Развернуть** → **Управление развертываниями**
2. Кликните на иконку ⚙️ у текущего развертывания
3. Выберите **Новая версия**
4. Нажмите **Развернуть**

---

## Использование в коде

### Автоматический трекинг (уже настроен)

```typescript
// В Layout.tsx - автоматически инициализируется
import { useAnalytics } from '../../../hooks/useAnalytics';

export const Layout = () => {
  useAnalytics(); // Сохраняет UTM и трекает просмотры страниц
  // ...
}
```

### Трекинг кнопок

```typescript
// В любом компоненте с Button
<Button
  color="primary"
  link="https://getcourse.ru/..."
  trackingGoal="workshop_purchase_click"
  trackingParams={{
    workshop: 'base',
    tier: 'live',
    price: '24990',
  }}
>
  Записаться
</Button>
```

### Кастомные цели

```typescript
import { trackGoal } from '../utils/analytics';

// Где угодно в коде
trackGoal('custom_goal_name', {
  param1: 'value1',
  param2: 'value2',
});
```

### Получение UTM-параметров

```typescript
import { getSavedUTMParams } from '../utils/analytics';

const utmParams = getSavedUTMParams();
console.log(utmParams);
// {
//   utm_source: 'telegram',
//   utm_medium: 'social',
//   utm_campaign: 'base_workshop_nov',
//   referrer: 'https://t.me/...',
//   landing_page: '/baza',
//   timestamp: '2024-11-18T12:34:56.789Z'
// }
```

---

## Примеры UTM-ссылок

### Telegram-канал (пост о воркшопе Base)

```
https://ru.sfer.ai/baza?utm_source=telegram&utm_medium=social&utm_campaign=base_workshop_nov&utm_content=channel_post
```

### Яндекс.Директ (контекстная реклама)

```
https://ru.sfer.ai/baza?utm_source=yandex&utm_medium=cpc&utm_campaign=search_ai_workshops&utm_term=ai_обучение
```

### Email-рассылка

```
https://ru.sfer.ai/baza?utm_source=newsletter&utm_medium=email&utm_campaign=weekly_digest_nov&utm_content=hero_cta
```

### VK (таргетированная реклама)

```
https://ru.sfer.ai/baza?utm_source=vk&utm_medium=cpc&utm_campaign=retargeting_base&utm_content=video_ad
```

### Instagram Stories

```
https://ru.sfer.ai/baza?utm_source=instagram&utm_medium=social&utm_campaign=stories_nov&utm_content=swipe_up
```

### Партнёрская ссылка

```
https://ru.sfer.ai/baza?utm_source=partner_blog&utm_medium=referral&utm_campaign=ai_review&utm_content=article_link
```

---

## Тестирование

### Шаг 1: Проверка сохранения UTM

1. Откройте сайт с UTM-параметрами:
   ```
   http://localhost:5173/?utm_source=test&utm_medium=manual&utm_campaign=debugging
   ```

2. Откройте **DevTools** (F12) → **Console**

3. Проверьте, что в консоли появилось сообщение:
   ```
   🎯 UTM-параметры сохранены: {...}
   ```

4. Проверьте sessionStorage:
   ```javascript
   console.log(sessionStorage.getItem('traffic_params'));
   ```

### Шаг 2: Проверка трекинга кликов

1. Кликните по кнопке "Записаться" на странице воркшопа

2. В консоли должно появиться:
   ```
   ✅ Метрика: цель достигнута workshop_purchase_click {...}
   ```

3. Откройте **Яндекс.Метрику** → **Отчёты** → **Стандартные отчёты** → **Содержание** → **События**

4. Проверьте, что события отображаются

### Шаг 3: Проверка отправки формы

1. Заполните форму заявки на вакансию

2. Отправьте форму

3. Проверьте консоль:
   ```
   ✅ Метрика: цель достигнута job_application_submitted {...}
   ```

4. Откройте Google Sheets и проверьте, что последняя строка содержит UTM-параметры

### Шаг 4: Проверка навигации

1. Перейдите между страницами сайта

2. В консоли должны появляться события:
   ```
   📊 Событие отправлено: page_view {...}
   ```

---

## Частые вопросы (FAQ)

### Как долго хранятся UTM-параметры?

UTM-параметры хранятся в **sessionStorage**, то есть:
- ✅ Сохраняются при переходах между страницами
- ✅ Сохраняются при перезагрузке страницы (F5)
- ❌ Удаляются при закрытии вкладки/браузера

Это правильное поведение: мы трекаем именно эту сессию, а не долгосрочные cookie.

### Что если пользователь зашёл без UTM?

Если в URL нет UTM-параметров:
- Данные просто не сохраняются
- В форме заявки UTM-поля будут пустыми
- В Метрику отправляются события без UTM
- Всё работает нормально

### Можно ли отследить несколько кампаний в одной сессии?

Нет, сохраняются только параметры первого захода. Если пользователь:
1. Зашёл с `utm_source=telegram`
2. Затем кликнул на ссылку с `utm_source=email`

Будет храниться `utm_source=telegram` (первый заход).

### Как посмотреть все цели в Метрике?

1. **Яндекс.Метрика** → **Отчёты** → **Стандартные отчёты** → **Конверсии**
2. Или: **Содержание** → **События** (для JavaScript-событий)

---

## Поддержка и контакты

Если у вас возникли вопросы по UTM-трекингу:

- **Документация Яндекс.Метрики**: https://yandex.ru/support/metrica/
- **UTM Builder**: https://ga-dev-tools.google/campaign-url-builder/
- **Техническая поддержка**: Kirill Gurbanov (gk@sfer.ai)

---

**Версия документа**: 1.0
**Дата создания**: 18.11.2024
**Последнее обновление**: 18.11.2024
